data_dir = "/tmp/"
log_level = "DEBUG"

datacenter = "dc1"
primary_datacenter = "dc1"

server = false

bind_addr = "0.0.0.0"
client_addr = "0.0.0.0"

retry_join = ["consul-server.container.shipyard.run"]