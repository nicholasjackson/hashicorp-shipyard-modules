# Name of the cluster to deploy charts to
consul_k8s_cluster = "dc1"

# Consul values file to overide defaults
consul_helm_values = "${file_dir()}/config/helm/consul-values.yaml"

# Name of the kubernetes cluster
monitoring_k8s_cluster = "dc1"

# Grafana values file to overide defaults
monitoring_helm_values_grafana = "${file_dir()}/config/helm/grafana-values.yaml"
